import React from 'react';

const Contacts = () => {
    return (
        <div>
            <h2>Контакты</h2>
            <form>
                <input/>
                <input/>
                <textarea/>
                <button>Отправить</button>
            </form>
            
        </div>
    );
};

export default Contacts;